I made it so it would get bot tokens from a .env file to increase security as to not show bot tokens in the the script.
I added a null checl to the get_status() since if it would return nothing, it would cause the script to error out.
I added a discription to the command for QOL.
I fixed the error with the spelling when importing discord.
