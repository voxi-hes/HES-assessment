This is a simple server diagnostic which is able to test wether the server is online and which ports are open. There is also an option to ssh into a server (this isnt fully tested since i still dont have my server). 
This is a batch file so it runs in your terminal, you may have to give it permissions to run (dependent on your pc finding it trust worthy or not). Ill attatch ss's.
